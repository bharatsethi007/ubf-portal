import PortalLogo from './PortalLogo'

/** UBF logo sized for portal nav (white JPG bg blends on white header). */
export default function PortalNavLogo() {
  return <PortalLogo className="portal-nav__logo" />
}
